<Window x:Class="WpfClient.AllProductsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClient"
        mc:Ignorable="d"
        Title="AllProductsView" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Margin="0, 0, 8, 8" Width="100" x:Name="butNew"  Content="Новая запись" />
            <Button Margin="0, 0, 8, 8" Width="160" x:Name="butEdit" Content="Редактировать запись" />
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="dGrid">

        </DataGrid>

    </Grid>
</Window>
